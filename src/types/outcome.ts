/** Produced only by `calculateOutcome(state)` in the Engine — never inferred elsewhere. */
export type Outcome =
  | "IN_PROGRESS"
  | "VICTORY"
  | "DEFEAT"
  | "TIME_LIMIT_REACHED"
  | "RESOURCES_EXHAUSTED";
